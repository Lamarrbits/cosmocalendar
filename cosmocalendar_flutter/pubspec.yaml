name: cosmocalendar_flutter
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # State Management
  flutter_riverpod: ^2.4.0

  # AI
  google_generative_ai: ^0.2.0

  # UI
  cupertino_icons: ^1.0.2

  # Internationalization
  flutter_localizations:
    sdk: flutter
  intl: ^0.18.0

  # Others
  go_router: ^12.0.0
  equatable: ^2.0.5
  flutter_dotenv: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
